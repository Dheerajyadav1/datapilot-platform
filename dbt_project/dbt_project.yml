name: "datapilot_platform"
version: "1.0.0"
config-version: 2

profile: "datapilot_platform"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:

  datapilot_platform:

    staging:
      +materialized: view

    gold:
      +materialized: table